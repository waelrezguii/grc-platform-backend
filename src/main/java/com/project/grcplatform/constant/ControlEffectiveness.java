package com.project.grcplatform.constant;

public enum ControlEffectiveness {
    INEFFECTIVE,      // 1 — control fails to reduce risk
    PARTIAL,          // 2 — control partially reduces risk
    LARGELY_EFFECTIVE, // 3 — control mostly reduces risk
    FULLY_EFFECTIVE   // 4 — control fully reduces risk
}