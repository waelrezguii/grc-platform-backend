package com.project.grcplatform.constant;

/** Performance du contrôle: how well the control operates. */
public enum ControlPerformance {
    PERFORMANT,              // Performant           — performing well
    MOYENNEMENT_PERFORMANT,  // Moyennement performant — average performance
    NON_PERFORMANT           // Non performant        — failing to perform
}
